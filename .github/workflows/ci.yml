name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install -r requirements.txt
          pip install pytest-cov

      - name: Install package
        run: pip install -e .

      - name: Run tests & produce coverage.xml
        run: |
          pytest --maxfail=1 --disable-warnings -q \
            --cov=flower_classifier --cov-report=xml

      - name: Upload coverage via bash uploader
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
        run: |
          # force upload into the nested slug path Codecov has already created
          bash <(curl -s https://codecov.io/bash) \
            -t $CODECOV_TOKEN \
            -r shaolinpat/shaolinpat/flower_classifier \
            -f coverage.xml

